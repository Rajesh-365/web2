import React from 'react';
import ResultsShowcase from '../components/ResultsShowcase';

/**
 * ResultsPage
 * - Standalone page for examination results
 * - Used with React Router as /results route
 * - Header and Sidebar are shown from the MainLayout
 */

const ResultsPage = () => {
  return <ResultsShowcase />;
};

export default ResultsPage;